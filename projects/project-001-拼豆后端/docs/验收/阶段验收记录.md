# 阶段验收记录

> 本文件由总调度在每阶段结束后追加，不以聊天内容替代。
> 当前状态：M0 技术验收已完成；M1 本地账号基础已完成。当前先完成本地最小功能，team-test 资源、DNS 和费用审批均延后至部署前检查通过后。

## 记录模板

```markdown
## Mx｜YYYY-MM-DD｜阶段结论

- 运行版本/分支：
- 已验证：
- 未完成：
- 测试证据：
- 数据/安全风险：
- 回滚点：
- 业务 Go / No-Go：
- 确认人：
```

## 当前阶段状态

| 阶段 | 状态 | 进入条件 | 验收状态 |
| --- | --- | --- | --- |
| M0 | 技术完成，等待业务 Go | 本地基础、M1 契约和供应商决策材料完成；进入 M1 须确认 team-test 资源授权 | 技术通过；业务 Go 待确认 |
| M1 | 本地账号、作品/文件、审计反滥用、Google Mock 与邮件投递准备完成，继续收口剩余能力 | M0 技术与业务验收通过 | 本地核心闭环通过；真实邮件投递、真实 Google OAuth 和 team-test 未验收 |
| M2 | 未开始 | M1 完成并验收，业务决定进入社区 | 未验收 |
| M3 | 未开始 | M2 通过或有明确可履约订单需求 | 未验收 |
| M4 | 未开始 | M3 证明人工订单可履约，业务决定进入定制 | 未验收 |

## M2.2｜2026-08-26｜社区治理后台后端本机闭环

- 运行版本/分支：`codex/m2-library-community`；Node `24.19.0`、pnpm `10.33.2`、Payload / PostgreSQL 适配器 `3.88.0`，仅本机 PostgreSQL 16。未修改 `/Users/alexwork/Documents/PixoMosaic`，未创建云资源、搜索平台或社交平台账号连接。
- 已验证：实现受控的 `/api/v1/admin/community/**` 后台接口，而非开放原始社区表。active Staff/Admin 可按全部状态读取社区帖子、冻结快照、保留社区媒体、举报、治理动作、用户社区资料、隐藏社交链接、内部备注和特别关注；响应不返回私密 Work/WorkDocument/WorkAsset、邮箱、认证集合、原图或 `storageKey`。用户可维护自己资料与白名单社交链接；访客只看 `public` 链接，后台可见真实值并带“隐藏”标记。精选/取消精选、下架/恢复、Admin 删除、举报处理、备注和特别关注均使用事务、幂等、版本冲突保护、不可变治理动作和最小审计；`shareable = published`、`indexable = published && featured` 由服务端派生。下架、撤回和删除会清除精选；恢复不自动恢复精选。公开/后台媒体均 `no-store`，下架后公开详情与媒体立即返回不可读投影。
- 测试证据：新增 M2.2 集成回归，覆盖普通用户后台拒绝、全状态后台查询、精选/下架/恢复派生、隐藏社交链接后台完整/访客隐藏、运营备注和特别关注隔离、待处理版权举报阻止精选、举报版本冲突；完整 `pnpm test` 为 **17 files / 69 tests passed**。同一 Node 24 下 `pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm build`、`pnpm migrate` 和 `git diff --check` 通过；在独立临时 PostgreSQL 数据库完成全量 `up → down（同批次） → up → status`，随后已删除该临时数据库。新迁移 `20260826_150000_m22_community_moderation`、`20260826_151000_m22_community_moderation_backfill`、`20260826_152000_m22_report_dedup_lock` 已在本机为 `Yes`。
- 未完成：前端后台管理页面、公开分享按钮、sitemap、结构化数据、SEO/GEO 公开页面、官方文章发布/审核、视频嵌入、外部平台发布与 MCP 服务身份均未实现或联调；本机派生 `indexable` 不等于已提交搜索引擎收录。
- 数据/安全风险：后台“完整”限定为社区域；隐藏社交链接仅在受控后台响应可见，不能写入搜索、SEO/GEO、MCP、公共缓存或日志。运营通知目前返回 `not_configured`/`not_requested`，没有真实邮件或站内通知发送。Admin 删除为逻辑状态变更；社区媒体和冻结快照的争议保留/物理清理调度尚未实现，不能将其表述为已有 30 天清理作业。
- 回滚点：两个 M2.2 显式迁移均有 down 路径；已运行开发库时应以受控 migration down/reapply 回滚，不得手改表。提交时只选择 M2.2 相关后端、测试与文档，不能混入 Workspace 根目录 `.playwright-cli/`、`.playwright-mcp/` 或库存规则历史改动。
- 业务 Go / No-Go：**M2.2 后端本机技术 Go；前端后台、公开 SEO/GEO/分享与任何线上部署继续 No-Go。**后续进入公开页前需先冻结前端接口、媒体保留/清理策略、运营通知文案与 Staff/Admin 名单。
- 确认人：总调度完成本机验证；业务方后续确认前端联调和公开内容阶段。

## M2.1-A｜2026-08-26｜官方内容草稿后台本机闭环

- 运行版本/分支：`codex/m2-library-community`；Node `24.19.0`、pnpm `10.33.2`、Payload / PostgreSQL 适配器 `3.88.0`，仅本机 PostgreSQL 16。未修改 `/Users/alexwork/Documents/PixoMosaic`，未创建云资源、搜索平台、MCP 服务身份或社交账号连接。
- 已验证：新增独立 `Article` 与 `ArticleMedia` 容器，文章仅可为 `draft`；`guides` 与 `blog` 类型对应受服务端校验。已实现 Staff/Admin 的草稿 `GET/POST/PATCH /api/v1/admin/content/articles`（详情为 `/:id`），支持来源清单、Lexical 正文、SEO 建议、幂等、乐观版本冲突、最小审计和独立限流。请求试图写入 `published`、`publishedAt` 或 `scheduledAt` 均返回 `409 CONTENT_STATE_INVALID`，不会静默发布或降级。Staff 可进入 Payload Admin 处理 Content，但经实际回归确认无法读取 users/accounts/sessions/verifications 或他人私密 Work；内容服务仅获得幂等表访问，不获得私密作品写权。
- 测试证据：`pnpm generate:types`、`pnpm lint`、`pnpm test`（16 files / 64 tests）、`pnpm build` 和 `pnpm migrate:status` 均通过；构建路由含两条后台内容路由。受控迁移 `20260826_042051_m21_content_drafts` 已在当前本机数据库为 `Yes`，并额外在隔离临时数据库完成全部迁移 `up → down → up → status`；迁移 JSON 快照与实际 `article_media.uploader → users` 的 `RESTRICT` 策略一致。
- 未完成：公开文章读取、审核/发布/排期、SEO/GEO、sitemap、分享按钮、视频嵌入、媒体上传/公开读取、MCP/API 服务身份、社区精选/下架与用户治理仍未实现；不能把本次结果表述为博客上线或 SEO 已启用。
- 数据/安全风险：草稿可含未发布选题与来源，只面向 Staff/Admin；文章媒体当前仅有元数据结构，绝不复用用户私密 Work/原图。Payload Admin 的全局入口为 Staff 放开后，认证内部集合仍由插件级 overrides 强制限为 Admin / Better Auth 内部请求；此约束有专项测试覆盖。
- 回滚点：本机无真实官方文章；迁移具有已验证的 down 路径。未提交、未部署且无外部状态需要回滚。提交时只选择 M2.1-A 内容后台相关文件，不能混入 Workspace 根目录 `.playwright-cli/`、`.playwright-mcp/` 或其他历史改动。
- 业务 Go / No-Go：**M2.1-A 本机草稿后台技术 Go；M2.1 发布/SEO/GEO 及 M2.2 社区治理继续 No-Go，须按对应 Spec 另行实现与验收。**
- 确认人：总调度完成本机验证；下一步由业务方审阅本次范围，再决定是否进入人工审核与公开内容阶段。

## M1.1｜2026-08-22｜PixoMosaic 云端作品与个人豆仓前端代码接入

- 运行版本/分支：后端 `codex/pixomosaic-m1-auth-foundation`，前端独立仓库分支 `codex/m1-cloud-save-inventory`；仅本机文件与数据库边界，未创建云资源或 DNS。
- 已验证：已获业务方授权修改前端并读取其《个人豆仓设计》。前端已接入统一 Cookie API client、单图/画板的主动保存和云端打开、服务端库存状态展示、`/inventory` 余额/账本/删除回滚、CSV 模板/预检/确认导入；本次补齐单图和画板“管理豆仓”、服务端缺货 CSV 导出、完成制作的服务端清单二次确认与账本结果展示，以及单图专心模式进入时的一次性库存提示。完成制作请求只传可选备注，不传用户 ID、颜色用量、本地库存、Data URL 或 IndexedDB 数据。
- 测试证据：前端已运行 `npx tsc --noEmit` 和 `git diff --check` 通过；此前已通过 `npx tsx tests/cloudWorkDocument.test.ts`、`npx tsx tests/boardDraftAndExport.test.ts`、`npx tsx tests/templatePatternImport.test.ts`、`npm run lint`、`npm run build`。本次文档更新后仍需统一重跑完整前端验证集。
- 未完成：尚未启动前后端进行真实浏览器 Cookie/CORS 往返，因而注册/登录、单图/画板保存、库存调整、CSV 预检/提交、删除回滚、完成制作及缺货导出的真实网络路径均不能称为完整联调；真实邮件、team-test、DNS 与任何云资源仍未开始。
- 数据/安全风险：前端仅展示服务端快照，未录入库存不会伪造为零；CSV 只支持 UTF-8 CSV，现有“漫漫 S4”歧义仍由后端预检阻断；美国市场色号体系在品牌、版本与来源确认前仍不实现。进入专心模式、作品保存和导出不会自动扣减库存。
- 回滚点：前端与后端分别保留独立 Git 工作树，未提交、未部署、未写 DNS；可选择性暂存本次相关文件，不能混入两个仓库原有未提交改动或 `tmp/`。
- 业务 Go / No-Go：**前端静态接入技术 Go；完整 M1.1 本地联调和 team-test 均 No-Go。**下一步是用本机后端与测试账号完成真实浏览器往返，再判断是否满足部署前检查。
- 确认人：总调度完成代码与静态核对；真实联调结果、云资源、预算与 DNS 仍待后续门禁。

## M1.1｜2026-08-23｜本机认证与个人豆仓 HTTP 往返验证

- 运行版本/分支：后端 `codex/pixomosaic-m1-auth-foundation`，前端独立仓库 `codex/m1-cloud-save-inventory`；使用独立本机开发实例 `127.0.0.1:3002`（后端）和 `127.0.0.1:3202`（前端），未创建云资源、DNS、真实邮件或 OAuth 凭据。
- 已验证：后端 `GET /health` 返回 200；来自前端本机来源的认证请求返回精确 `Access-Control-Allow-Origin` 和 Cookie 凭据许可。一次性随机本机账号已完成注册、邮箱 OTP 验证、密码登录、`get-session` 会话读取；随后以真实 HTTP Cookie 调用库存路由，验证 `2.6mm + #FAF4C8 = 100（正常）` 与 `5mm + #FAF4C8 = 49（缺货）` 相互隔离，入库操作可删除回滚且原操作从用户历史隐藏；`漫漫 S4` CSV 预检按预期以 `AMBIGUOUS_COLOR_CODE` 拒绝。测试账号凭据、OTP、Cookie 均未写入文件、日志或本记录。
- 测试证据：临时、未纳入 Git 的同进程账号准备测试通过；再以真实 `3002` HTTP 服务跑完 Cookie/CORS/库存闭环，结果为 `M11_LOCAL_HTTP_COOKIE_AND_INVENTORY_VALIDATED`。前端已通过 `npx tsc --noEmit`、`npx eslint src tests`、`npm run build` 与 `git diff --check`；后端既有 M1.1 自动化测试已通过。浏览器技能策略在重新加载本机页面时拒绝导航，未绕过该限制。
- 未完成：因浏览器策略拒绝后续本机页面导航，未能以页面点击方式复核已登录 `/inventory` 的视觉状态、CSV 文件选择/下载事件或作品制作扣减页面；前端代码与真实 HTTP 契约已验证，但这些浏览器 UI 动作不能标为已通过。真实邮件、Google OAuth、team-test、DNS 和任何云资源仍未开始。
- 数据/安全风险：本地联调实例仅用于开发；注册与库存真值仍由后端数据库和 HttpOnly Cookie 管理。浏览器登录页是 M1 最小联调入口，不等同于密码重设、Google 登录或公开注册产品体验完成。
- 回滚点：独立实例可停止，临时构建目录不纳入 Git；本机测试数据不用于交付。无云端状态或 DNS 变更需要回滚。
- 业务 Go / No-Go：**认证与豆仓的本机 HTTP 集成技术 Go；浏览器 UI 全流程、作品制作扣减 UI 与部署前检查仍未验收，team-test 继续 No-Go。**
- 确认人：总调度完成本机 HTTP 验证；云资源与部署仍须业务方在费用说明后确认。

## M1.1｜2026-08-23｜真实浏览器本机闭环与 A/B 隔离

- 运行版本/分支：后端 `codex/pixomosaic-m1-auth-foundation`，前端独立仓库 `codex/m1-cloud-save-inventory`；后端 `127.0.0.1:3002`、前端 `127.0.0.1:3202`，未创建云资源、DNS、真实邮件或 OAuth 凭据。
- 已验证：A 用户在真实浏览器完成注册、邮箱 OTP 验证、密码登录、进入个人豆仓并逐色入库 `2.6mm/#FAF4C8 +100`；随后删除入库记录并确认回滚，页面显示余额 0/缺货，原操作隐藏且回滚记录保留。A 用户上传 2×2 PNG、生成图纸、填写名称并主动保存云端，网络记录为 `POST /api/v1/works 201`、`PATCH /document 200`，页面显示“云端版本 1”。A 用户点击“完成制作并扣减”，浏览器显示服务端二次确认清单，确认后 `POST /complete 200`，页面显示扣减结果；再从历史删除制作记录，`DELETE /operations/:id 200`，两种颜色余额恢复为 0。B 用户使用独立浏览器会话注册、验证、登录后，个人豆仓显示“尚无符合条件的库存”，云端作品弹窗显示“还没有云端作品”，无法读取 A 数据。
- 接口证据：认证、库存、作品保存、库存状态、制作扣减和回滚均为真实跨源 Cookie HTTP 请求；成功/错误响应按当前接口契约返回，浏览器未读取或提交 owner、用户 ID、本地库存、Data URL 或颜色用量。
- 独立测试证据：后端 `pnpm test` 为 12 files / 48 tests，`pnpm lint`、`pnpm build` 通过；前端 `npx tsc --noEmit`、`npx eslint src tests`、`npm run build`、三项转换/导入测试和 `git diff --check` 通过。后端临时 `.next-*` 构建目录已加入忽略规则，避免生成物污染 lint。
- 未完成：CSV 文件选择/下载事件、真实邮件投递、真实 Google OAuth、team-test、DNS 和部署前费用 Go 未验收；未把这些范围标记为完整前后端联调或上线就绪。
- 数据/安全风险：本轮浏览器账号为一次性本机测试账号，凭据、OTP、Cookie 未写入文档；本机数据库仍有测试余额/作品，不能用于交付。`tmp/` 与 `.next-*` 构建目录不进入提交。
- 回滚点：未执行 Git commit、未部署、未写 DNS；停止本机进程并清理测试数据库/临时构建目录即可回到无本机联调状态。
- 业务 Go / No-Go：**M1/M1.1 本机浏览器闭环技术 Go；team-test、真实外部服务和生产仍 No-Go。**本记录不把本机 HTTP 结果写成完整线上联调。
- 详细分层、接口映射、命令和证据：`docs/验收/M1.1-改动分层-接口对照-独立测试-浏览器闭环-2026-08-23.md`。

## M1.1｜2026-08-23｜浏览器剩余库存门禁补验

- 运行版本/分支：后端 `codex/pixomosaic-m1-auth-foundation`，前端 `codex/m1-cloud-save-inventory`；后端 `127.0.0.1:3002`、前端 `127.0.0.1:3202`，仍为本机独立实例。
- 已验证：未登录独立浏览器会话访问 `/inventory` 时只显示登录引导，不渲染读取、导入、调整或扣减控件。已登录 A 会话点击模板下载，`GET /api/v1/inventory/template` 返回 200 并产生浏览器下载事件；选择 CSV 后成功预检返回 201 并显示确认按钮。把色号系统切换为“漫漫”后预检 `S4,10` 返回 422，页面逐行提示“色号对应多个颜色，无法安全导入”。成功预检后先调整同一颜色余额，再确认旧预检，`POST /imports/commit` 返回 409，页面提示重新预检，旧预览没有覆盖新余额。临时 `+5` 调整已通过真实页面删除回滚，余额回到基线。
- 测试证据：后端完整验证重新运行，`pnpm test` 为 12 files / 48 tests，`pnpm lint`、`pnpm build` 与 `GET /health` 均通过；前端 `npx tsc --noEmit`、`npx eslint src tests`、`npm run build`、三项转换/导入测试和 `git diff --check` 均通过。
- 未完成：429 仅由后端并发限流测试覆盖，未在浏览器人为制造高频压力；真实邮件、真实 Google OAuth、team-test、DNS、云存储和生产部署均未验收。
- 数据/安全风险：浏览器验证使用一次性本机会话和本机数据库；没有凭据、Cookie 或 OTP 写入文档。该环境不接收真实用户数据。
- 回滚点：未执行 Git 提交、未创建云资源；本机临时构建目录与测试数据仍不纳入交付。
- 业务 Go / No-Go：**M1/M1.1 本机浏览器 HTTP 技术 Go；team-test 和生产仍 No-Go。**
- 确认人：总调度完成本机补验；外部环境仍须在费用说明和资源授权后单独验收。

## M0｜2026-08-19｜技术基础完成，等待 M1 业务 Go

- 运行版本/分支：`codex/pixomosaic-backend-foundation`；Node `24.19.0`（项目 `.node-version`）、pnpm `10.33.2`、Payload/`@payloadcms/db-postgres` `3.88.0`、PostgreSQL 16。本仓库尚无初始 Git 提交，本次不自动提交。
- 已验证：本地 PostgreSQL 容器仅绑定 `127.0.0.1:55440` 且无用户数据；显式初始迁移 `20260818_161323_init_users` 已运行；`/health` 本地和 Docker 生产模式均返回 200；GraphQL/模板 Media/示例路由已关闭或移除；自动 schema push 已禁用；服务 README、接口冻结、供应商与权限材料已更新。
- 未完成：M1 账号、作品、资产、权限、邮件、OAuth、反滥用、真实前端往返联调与 team-test 演练均未开始；未创建任何云账号、桶、DNS 或部署。
- 测试证据：`pnpm lint` 通过；`pnpm test` 通过（1 passed）；`pnpm build` 通过；`pnpm audit --prod` 为 0 vulnerabilities；`pnpm migrate:status` 显示初始迁移已运行；Docker Node 24 standalone 镜像构建并以本地 PostgreSQL 运行，`/health` 返回 200。
- 数据/安全风险：Payload 默认访问控制不适合多人环境；M1 必须覆盖普通用户、Staff、Admin 与作品归属权限。Payload REST/Admin 仍是管理面，M1 必须隔离业务 `/api/v1`、CORS/CSRF/Cookie 白名单和 Admin 访问。真实价格、区域和免费额度会变化，开通当日重新核对。
- 回滚点：M0 未写入真实业务数据，也未创建远程资源；可停止本地容器并从已锁定依赖、显式迁移和 Docker 镜像重新构建。任何 M1 schema 变更必须添加可评审迁移，不得开启自动 schema push。
- 业务 Go / No-Go：**技术 Go，业务待确认。**在业务方确认 team-test 的推荐组合或替代、主账号、预算、区域、`api-test.<主域名>` DNS 与测试发件域前，保持 No-Go，不进入 M1。
- 确认人：业务方待确认；总调度完成技术收口。

## M1｜2026-08-19｜本地账号基础完成，等待完整 M1 输入

- 运行版本/分支：`codex/pixomosaic-m1-auth-foundation`；Node `24.19.0`、pnpm `10.33.2`、Payload `3.88.0`；数据库仍为本机 PostgreSQL 16，无真实用户数据。
- 已验证：用户角色、账号状态、认证来源、条款字段已加入；邮箱验证、登录失败锁定、Cookie、CORS/CSRF 环境边界已配置；显式迁移 `20260818_232836_m1_user_account_fields` 已执行；配置与管理面隔离测试通过（5 tests passed）。
- 已完成复用核查：Google 登录候选为 `payload-auth@3.0.0 + better-auth@1.7.1`；未安装、未接入真实 OAuth，需先通过本地 PoC。
- 未完成：注册/验证/重设 API、作品/资产模型、R2、Resend、反滥用限流、Admin/Staff 业务权限、真实 Google 回调、前端联调和 team-test 部署。
- 测试证据：`pnpm lint` 通过；`pnpm exec tsc --noEmit` 通过；`pnpm test` 通过（2 files / 5 tests）；`pnpm audit --prod` 无已知漏洞；迁移执行成功。
- 数据/安全风险：当前仅是本地基础，Payload 默认 REST/Admin 仍不是前端业务 API；Google 插件会替换 Local Auth 并新增集合，PoC 通过前不得安装或部署。
- 回滚点：本地无用户，可回退 `20260818_232836_m1_user_account_fields`；不对 team-test 或生产执行迁移。
- 业务 Go / No-Go：**本地基础技术 Go；完整 M1 No-Go。**当前先完成本地功能；不创建云资源、不配置真实邮件或 OAuth、不添加 DNS。部署前必须说明当日费用、免费额度、预算上限和停止/删除方案，再由业务方确认是否创建 team-test。
- 确认人：总调度完成本地基础；业务方待提供 team-test 信息。

## M1｜2026-08-19｜team-test 延后至本地功能完成后

- 已确认：主域名为 `pixomosaic.com`；当前不创建 Railway、Neon、R2、Resend 或 Google OAuth 资源，不添加 `api-test.pixomosaic.com` CNAME，不需要 Cloudflare 密码或 API Token。
- 新执行顺序：先完成本地注册、作品、上传、邮件模拟、Google 本地 PoC、权限与反滥用验证；部署前检查通过后，重新核实费用和免费额度并请业务方确认；Railway 分配实际域名后，业务方自行添加 CNAME；最后只向邀请/白名单团队开放 team-test。
- 成本边界：资源创建前必须说明预估月费用、免费额度、预算上限、超额触发点和停止/删除方案；未获当次业务 Go 不得开户、部署或写 DNS。
- 关联：`../实施准备/M1-team-test-部署前检查与费用Go.md`。

## M1｜2026-08-20｜本地邮箱认证闭环完成

- 运行版本/分支：`codex/pixomosaic-m1-auth-foundation`；Node `24.19.0`、pnpm `10.33.2`、Payload `3.88.0`、`payload-auth 3.0.0`、Better Auth `1.7.1`，仍只使用本机 PostgreSQL 16。
- 已验证：独立必填的 `BETTER_AUTH_SECRET`；邮箱密码注册、邀请/白名单判断、一次性验证码邮件验证、密码登录、忘记/重设密码、登录失败锁定、停用账号拒绝、CORS/CSRF 和登出/会话底层能力。未验证邮箱不能密码登录；验证成功会同时成为 `active`；连续 5 次错误密码锁定 15 分钟；重设令牌单次消费并撤销重设前会话。
- 邮箱验证安全：不采用 Better Auth 默认的可在有效期内重复使用的 JWT 链接；采用其维护的 `emailOTP` 插件，15 分钟有效、数据库只保存验证码哈希、验证码原子消费、最多 3 次验证码错误。项目路由显式拒绝该插件附带的免密码登录、OTP 重设等超范围入口，邮箱密码仍是唯一登录方式。
- 迁移与测试证据：`20260819_101810_m1_better_auth` 与 `20260820_143322_m1_login_security` 已顺序执行；`pnpm migrate:status` 显示 4 条迁移均为 `Yes`；`pnpm test` 为 3 files / 10 tests passed；`pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm build`、`git diff --check` 均通过。
- HTTP 复核：固定本机 `127.0.0.1:3002`，`GET /health` 为 200；受信来源预检为 204，未知来源为 403；OTP 免密码登录端点为 404。
- 已处理工具兼容：`payload-auth 3.0.0` 发布的 ESM 产物存在无扩展名相对导入，Vitest 需只在测试中内联转换该依赖；生产 Next 构建不受影响。
- 未完成：密码重设 HTTP 闭环、真实邮件适配器、审计、作品/资产、A/B 权限、Google mock、真实前端联调和 team-test 均未完成；不创建任何云资源、DNS 或 OAuth 凭据。
- 数据/安全风险：本地验证数据仅存在本机数据库，测试中只使用随机的示例邮箱；本地 outbox 仅用于测试，不能在 team-test/生产启用。登录锁定是基于账号的 15 分钟防护，仍需在后续反滥用阶段补充 IP/设备维度审计和监控。
- 回滚点：认证结构有受控增量迁移；在本机无真实数据前可按迁移顺序回滚，不能用自动 schema push。未存在可回滚的云端资源。
- 业务 Go / No-Go：**本地邮箱认证技术 Go；完整 M1 仍 No-Go。**下一步仅实现本地作品、资产与权限；完成全部本地功能和 Docker 部署检查后，再重新提供费用与停用方案并请求 team-test 创建授权。
- 确认人：总调度完成本地认证闭环；云资源、真实邮件/OAuth 与 DNS 仍待业务方后续确认。

## M1｜2026-08-20｜私密作品数据模型与会话隔离基础完成

- 运行版本/分支：`codex/pixomosaic-m1-auth-foundation`；Node `24.19.0`、pnpm `10.33.2`、Payload `3.88.0`，仅本机 PostgreSQL 16。
- 已验证：新增受控的 `Work`（归属、状态、当前快照）、不可变 `WorkDocument`（版本化 JSON 真值）、私有 `WorkAsset`（文件元数据）和持久化幂等记录。作品 ID 采用不暴露 Payload 数字主键的 `publicId`；原始 Payload REST 端点全部关闭，Admin/Staff 不默认浏览普通用户私密作品。
- 会话边界：业务服务通过 Better Auth 的数据库权威会话查询取得当前用户；不解析 Cookie、不接受前端 `userId`。未登录、未验证或非 active 账号不能取得私密资源本地请求；敏感操作绕过 Cookie 会话缓存，使停用/撤销即时生效。
- 迁移与测试证据：已生成、审查并执行 `20260820_153723_m1_work_models`；`pnpm migrate:status` 显示 5 条迁移均为 `Yes`。本机数据库已核对四张新表、作品历史唯一索引、资产/幂等索引及 `RESTRICT` 清理外键。`pnpm test` 为 4 files / 12 tests passed；`pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm build` 和 `git diff --check` 均通过。
- 未完成：图纸 JSON 深度校验、`POST/GET /api/v1/works`、A/B 实际端点隔离、50 个 active 上限、修订冲突、上传本地模拟、删除回收、审计和 Google mock。当前数据模型不等于作品 API 已可供前端联调。
- 数据/安全风险：Payload 自动生成的关系默认不能表达业务回收顺序，因此迁移已将用户/作品到历史与资产的删除改为 `RESTRICT`；未来回收任务必须先清理受控文件和历史快照，不能依赖级联删除。所有写操作仍须使用数据库幂等记录，不能退化为内存 Map。
- 回滚点：本机无真实作品；该迁移有受控 down 路径。不得开启自动 schema push，也不得对 team-test/生产环境提前执行。
- 业务 Go / No-Go：**作品模型技术 Go；完整 M1 仍 No-Go。**下一步仅实现 2×2 `pattern` 服务端校验和最小私密作品接口；不创建云资源、DNS、真实邮件或 OAuth 配置。
- 确认人：总调度完成本地模型与迁移闭环；部署资源仍待业务方后续确认。

## M1｜2026-08-21｜pattern draft 创建接口完成

- 运行版本/分支：`codex/pixomosaic-m1-auth-foundation`；Node `24.19.0`、pnpm `10.33.2`、Payload `3.88.0`，仅本机 PostgreSQL 16。
- 已验证：`POST /api/v1/works` 只允许已验证且 active 的 Better Auth 会话；请求必须来自可信 Origin、使用 JSON、包含 `Idempotency-Key`。服务端接受契约的 2×2 `pattern` 样本，创建 private draft Work、不可变 revision 0 WorkDocument 和当前文档指针；公开响应仅返回 `workId`，不暴露 Payload 数字 ID、owner、存储键或内部文档 ID。
- 图纸校验：固定 `schemaVersion=1`、`kind=pattern`、标题 1–120 字符、矩形矩阵、单图最大 300×300/90,000 格、严格大写 HEX、规范透明格、色号统计/颗数重算比对和解码后 8 MiB 上限。失败使用 `WORK_DOCUMENT_INVALID` 或 `WORK_DOCUMENT_TOO_LARGE`，不修正、不截断客户数据。
- 幂等与事务：同一用户、路由和幂等键的重复请求返回第一次成功结果，不重复创建作品；不同请求复用同一键返回冲突。幂等记录、作品、首份快照和当前指针在同一 Payload/PostgreSQL 事务中写入，失败回滚。
- CORS/CSRF：业务 API 不复用认证路由的 Header 集合；写入必须匹配登记 Origin，预检明确允许 `Idempotency-Key`。本机可默认使用自身 `AUTH_BASE_URL`，team-test/生产不会继承此放宽，仍需显式来源白名单。
- 验证证据：`pnpm migrate:status` 为 5 条迁移均 `Yes`；`fnm exec --using ../../.node-version pnpm test` 为 5 files / 16 tests passed；同一 Node 24 下的 `pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm build` 和 `git diff --check` 均通过。
- 未完成：`GET /api/v1/works`、详情读取和 A/B 实际端点隔离、文档更新/激活及 50 active 上限、`board`、受控文件模拟、删除回收、审计、限流、Google mock 与前端联调。**完整 M1 仍 No-Go。**
- 业务与资源边界：没有创建 Railway、Neon、R2、Resend、Google OAuth、Cloudflare/DNS 或任何线上服务；未修改 PixoMosaic 前端仓库。

## M1｜2026-08-21｜pattern 读取与激活更新本地闭环

- 运行版本/分支：`codex/pixomosaic-m1-auth-foundation`；Node `24.19.0`、pnpm `10.33.2`、Payload `3.88.0`，仅本机 PostgreSQL 16。
- 已验证：`GET /api/v1/works` 和 `GET /api/v1/works/:id` 只投影当前用户自己的 active 作品；草稿和其他用户作品一律返回不可访问。`PATCH /api/v1/works/:id/document` 已可把 pattern draft 激活为 active，或追加 active 作品的不可变 WorkDocument 快照；Work 的 currentDocument、修订号和 SHA-256 与快照一致。
- 并发与配额：新增并执行受控迁移 `20260821_215000_m1_work_revision_guard`。数据库触发器强制修订号只能逐次递增，并在首次激活时锁定用户行、限制每用户最多 50 份 active 作品；服务层以 `WHERE revision = expectedRevision` 条件更新避免两个请求同时把 revision 0 写成 1。旧修订、并发第二请求和第 51 个激活均返回 409，失败事务不会留下新快照或幂等成功记录。
- 迁移快照修正：Payload 3.88 的单关系字段默认推导为 `SET NULL`，不符合用户、作品和历史快照的受控回收边界。已通过配置期 schema 钩子明确保留 owner/history 的 `RESTRICT`，仅 Work.currentDocument 为 `SET NULL`；修正 `20260821_130300` 的 JSON 快照，核对本机数据库外键未被误改。
- 测试证据：`pnpm migrate:status` 显示 6 条迁移均为 `Yes`；`fnm exec --using ../../.node-version pnpm test` 为 5 files / 21 tests passed，覆盖 draft→active、内容哈希、材料清单修订、幂等重试、A/B 隔离、旧修订、并发更新、PATCH CORS 和 50 个上限；同一 Node 24 下 `pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm build` 与 `git diff --check` 待本次文档更新后统一复跑。
- 未完成：上传意图/确认、本地对象存储模拟、资产引用校验、删除与回收、审计、反滥用、board、真实邮件/Google 和真实前端联调。完整 M1 与 team-test 仍为 No-Go；未创建云资源、DNS 或 OAuth。

## M1｜2026-08-22｜本地受控文件与并发修复闭环

- 运行版本/分支：`codex/pixomosaic-m1-auth-foundation`；Node `24.19.0`、pnpm `10.33.2`、Payload `3.88.0`，仅本机 PostgreSQL 16。
- 已验证：`upload-intent → PUT → confirm → GET` 闭环支持 PNG/JPEG/WebP。每次上传重新校验活动会话、可信 Origin、作品和 owner；服务端以限长分块读取、真实文件特征、Sharp 解码、SHA-256 与长度校验阻止伪造 MIME、超限和篡改。重复相同 PUT 返回 204 不覆盖对象，内容不同的重试返回 409；ready 文件只对本人返回，带下载附件和 `nosniff` 响应头，不泄露存储键或本机路径。每作品 10 张/100 MiB、每用户 2 GiB 预留计入数据库；可用 `pnpm cleanup:assets` 清理过期 `upload_pending` / `uploaded` 与无效资产。
- 并发修复：不同幂等键同时更新同一作品曾出现 PostgreSQL `40P01` 死锁，根因是幂等 actor 外键持有用户共享锁后，active 数量触发器再尝试对同一用户行 `FOR UPDATE`。新增并往返验证受控迁移 `20260821_231500_m1_active_work_advisory_lock`，改用按 owner 的事务 advisory lock 串行化 active 数量计算；并发更新稳定为一个 200、一个 409，不再返回 500。
- 迁移与测试证据：最后一条迁移已执行 `migrate:down → migrate → migrate:status`，8 条均为 `Yes`；资产专项为 5 tests passed。文档同步后的完整复核已完成：集成测试 6 个文件 / 29 tests 全部通过（含删除与到期回收专项），`pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm build`、`git diff --check` 均通过；生产 Docker 镜像 `pixomosaic-backend:m1-local` 构建成功，运行期注入真实格式的临时密钥后 `/health` 返回 200，容器健康检查为 healthy。镜像检查未发现 `BETTER_AUTH_SECRET` 被写入，构建期占位标记未进入 runner。
- 已完成收口：`DELETE /api/v1/works/:id/draft` 和 `POST /api/v1/works/:id/deletion-request` 已实现；草稿立即隐藏，active 作品进入 30 天回收期；`pnpm cleanup:works` 按对象→资产→快照→作品顺序执行到期物理回收。删除专项覆盖草稿取消、active 删除、幂等重试、跨用户隔离和旧修订冲突。
- 未完成：`PATCH /document` 对 ready 资产的同作品/同 owner 引用校验、board、审计/反滥用、真实邮件、Google mock、真实前端联调和 team-test。完整 M1 与 team-test 均仍为 No-Go；未创建云资源、DNS 或 OAuth。
- 数据/安全风险：本机对象目录只适用于受控自动化验证，不能作为 Docker/Railway 持久化存储；未来接 R2/S3 仅替换存储适配器，不能放松 API、归属、哈希、配额或私有读取规则。
- 回滚点：本机无真实业务数据；最新迁移可受控 down 后重新 migrate，文件对象可由本项目内的清理入口删除。没有云资源需要停止或删除。
- 业务 Go / No-Go：**本地文件与作品核心能力技术 Go；完整 M1 和 team-test 仍 No-Go。**
- 确认人：总调度完成本机闭环；部署资源、预算和域名仍待业务方后续确认。

## M1｜2026-08-22｜board v1 与受控资产引用本地闭环

- 运行版本/分支：`codex/pixomosaic-m1-auth-foundation`；Node `24.19.0`、pnpm `10.33.2`、Payload `3.88.0`，仅本机 PostgreSQL 16。
- 已验证：`POST /api/v1/works` 现可创建严格校验的 `pattern` 或不含资产 ID 的 `board` draft；`PATCH /api/v1/works/:id/document` 可保存 `board` v1、原子激活为 active 并追加不可变快照。画板校验覆盖 20 层、单层 90,000 格、总图层 180,000 格、坐标/边界、唯一 layer ID / zIndex、避让模式重叠、稀疏编辑 20,000 上限、换色 256 上限、直接/擦除冲突、换色链/循环、材料清单和顶层修订。
- 资产引用：只在已冻结的 board 图层字段开放；`sourceAssetId` 必须是当前用户、当前作品、`ready`、`original`，`thumbnailAssetId` 必须是当前用户、当前作品、`ready`、`thumbnail` 或 `display`。跨用户、跨作品、`uploaded` 未确认、错误角色及不存在资产一律返回 `ASSET_NOT_READY`，写入事务回滚，draft 不会被激活。`pattern` 因契约没有资产字段而保持拒绝。
- 测试证据：新增 `tests/int/m1-board-document.int.spec.ts`，覆盖契约校验、同作品成功、幂等重试、跨作品、跨用户、未确认与错误角色；`pnpm test:int` 为 7 个文件 / 32 项通过，`pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm build` 通过；构建路由清单包含正式 `/api/v1/works/[id]` 写入口；`pnpm migrate:status` 8 条迁移均为 `Yes`。
- 未完成：真实 PixoMosaic 往返联调、`pattern` 资产字段/兼容设计、审计和登录/上传/写入限流、真实邮件、Google 本地 mock 与 team-test 均未完成；未创建云资源、DNS 或 OAuth。
- 数据/安全风险：本机对象目录仍是自动化测试替身，不得用于 Docker 或云端持久化。只有在接口契约经前端联调确认后，才考虑在兼容版本中新增 pattern 资产字段；不能以临时字段破坏已冻结的 v1。
- 回滚点：本次只改变服务端校验、业务查询与测试，没有新迁移；本机无真实业务数据。可由当前 M1 基线重新构建并以已有受控迁移启动。
- 业务 Go / No-Go：**board 与受控资产引用本地技术 Go；完整 M1 和 team-test 仍 No-Go。**
- 确认人：总调度完成本机验证；真实前端联调与云资源仍待业务方后续确认。

## M1｜2026-08-22｜审计与反滥用本地闭环

- 运行版本/分支：`codex/pixomosaic-m1-auth-foundation`；Node `24.19.0`、pnpm `10.33.2`、Payload `3.88.0`，仅本机 PostgreSQL 16。
- 已验证：保留 Better Auth 的注册、登录、重设数据库限流及 5 次错误密码锁定；新增受控迁移 `20260822_103000_m1_audit_and_abuse_controls`，以 actor + scope + 时间桶的 PostgreSQL 原子 upsert 为作品写入、上传 intent、PUT、confirm 和私有下载限速。PUT 在读取二进制前先限流；达到上限响应 `429 RATE_LIMITED` 与 `Retry-After`，不会创建新业务数据或读取文件。
- 审计边界：创建/保存/删除作品、上传 intent/PUT/confirm、私有下载以及已知账号认证关键动作写入最小事件；成功业务写入与审计处于同一事务，下载先审计后返回。记录只含操作者内部 ID、固定动作/路由、公开资源 ID、请求 ID、结果/原因码和时间，不保存邮箱、密码、OTP、Cookie、Token、IP、完整 URL、作品内容、哈希、存储键或本机路径。认证与二进制下载均返回 `X-Request-Id`。
- 清理与保留：新增 `pnpm cleanup:security`，仅清理 90 天审计、2 天业务限流桶与超期 24 小时的幂等缓存；不触及用户、会话、作品或对象文件。
- 测试证据：新增 `tests/int/m1-audit-and-rate-limit.int.spec.ts`，验证最小审计无作品/会话内容、同一用户上传桶第 16 次原子限流；文档同步后运行 `pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm test:int`（8 文件 / 34 项）、`pnpm build`、`pnpm migrate:status`（9 条均 `Yes`）和 `git diff --check` 全部通过。
- 未完成：真实 PixoMosaic 往返联调、`pattern` 资产字段/兼容设计、真实邮件、Google 本地 mock、Admin/Staff 业务后台能力与 team-test 均未完成；未创建云资源、DNS 或 OAuth。
- 数据/安全风险：应用级本地规则按活动用户计数，不收集或信任 IP；它不能替代公开互联网部署后的可信反向代理、请求大小、慢请求、IP/WAF 和异常监控边界。team-test 前必须重新确认阈值、保留期、可信代理策略与预算。
- 回滚点：本机无真实业务数据；新增迁移含受控 down，安全记录可由本项目清理入口按保留期删除。未创建云资源。
- 业务 Go / No-Go：**本地审计与反滥用技术 Go；完整 M1 和 team-test 仍 No-Go。**
- 确认人：总调度完成本机验证；部署环境规则和费用仍待业务方后续确认。

## M1｜2026-08-22｜邮件 team-test 配置门禁与前端只读契约核对

- 运行版本/分支：`codex/pixomosaic-m1-auth-foundation`；Node `24.19.0`、pnpm `10.33.2`、Payload `3.88.0`，只使用本机 PostgreSQL 16；未改动 PixoMosaic 前端仓库。
- 已验证：`MAIL_TRANSPORT=local-outbox` 只在 `APP_ENV=local` 使用无害本机发件人占位；`APP_ENV=team-test` 的 `MAIL_TRANSPORT=resend` 现在必须显式提供 `RESEND_API_KEY`、`MAIL_FROM_ADDRESS` 和 `MAIL_FROM_NAME`，缺任一项即拒绝启动，不会继承本机占位值。新增配置专项测试覆盖缺项和完整受控配置。已对前端基线 `c6a4b04` 进行只读核对：`pattern` 与 `board` 的可编辑 JSON 均可经转换层映射到 v1；现有前端未发现 `/api/v1`、上传、Cookie 或幂等客户端，浏览器本地库/Data URL/草稿 revision 不能直接作为云端协议。
- 测试证据：`pnpm migrate:status` 显示 9 条迁移均为 `Yes`；`pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm test:int`（9 文件 / 39 项）、`pnpm build`、`pnpm audit --prod`（无已知漏洞）和 `git diff --check` 全部通过。首次 Docker 最终镜像构建发现：Next 在 builder 阶段以 production 运行，原有占位分支错误依赖非 production 条件，因而拒绝构建；已收敛为仅 Dockerfile 显式注入的 `PIXOMOSAIC_BUILD_PHASE=1` 才允许无密钥构建占位，runner 仍必须提供真实密钥。修复后生产镜像 `pixomosaic-backend:m1-local-final` 构建成功，以本机 PostgreSQL 和一次性非敏感健康检查变量启动后 `/health` 为 200，Docker healthcheck 为 `healthy`；检查后已停止容器。
- 未完成：真实邮件投递、真实 PixoMosaic 浏览器往返联调、`pattern` 资产兼容设计、Admin/Staff 业务后台能力与 team-test。未创建 Railway、Neon、R2、Resend、Google OAuth 或 DNS。
- 数据/安全风险：本地发件人占位只用于不出进程的 outbox；不得在 team-test 依赖它。前端目前 300 ms 本地草稿自动保存不能直接变成云端自动保存，M1 必须保持用户主动保存与 `expectedRevision` 冲突处理。
- 回滚点：本次仅收紧运行时配置、增加配置测试和项目内只读核对记录，无迁移和无云端资源；可按当前分支重新构建。
- 业务 Go / No-Go：**本地邮件门禁与前端契约核对技术 Go；完整 M1 与 team-test 仍 No-Go。**真实邮件和真实前端往返必须分别验收后，才可按部署前费用 Go 流程请求云资源授权。
- 确认人：总调度完成本地配置与只读核对；云资源、预算、测试邮箱和真实前端实现仍待业务方后续确认。
